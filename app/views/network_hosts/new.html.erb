<header class="page-header">
  <div>
    <h1>Add a network host</h1>
    <p class="subtitle">Register a device in your network inventory.</p>
  </div>
</header>

<%= render "form" %>

<p><%= link_to "← Back to all hosts", network_hosts_path, data: { turbo_frame: "_top" } %></p>
